# 🎯 Self-Orchestrating Agent

**自主化Agent系统** - 整合8个核心技能的完整自动化编排

## 📦 整合技能

| 基础技能 | 功能 | 角色 |
|---------|------|------|
| `autonomous-learning` | 自主学习 | 获取新能力 |
| `proactive-agent` | 主动代理 | 机会检测与行动 |
| `self-evolving` | 自我进化 | 三层进化机制 |
| `self-healing` | 自我修复 | 健康监控恢复 |
| `self-improvement` | 自我改进 | 质量反馈循环 |
| `beads-agent` | 任务管理 | Git-backed持久化 |
| `coding-agent` | 编程代理 | 代码任务执行 |
| `feedback-loop` | 质量保证 | 测试与验证 |

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                   Self-Orchestrator                        │
│                    (主协调循环)                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Health   │  │ Learning │  │ Evolving │  │  Tasks   │   │
│  │ 循环     │  │ 循环      │  │ 循环      │  │  循环    │   │
│  │ 1小时    │  │ 4小时     │  │ 2小时     │  │ 30分钟   │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       │             │             │             │          │
│       └─────────────┴─────────────┴─────────────┘          │
│                         │                                  │
│  ┌──────────┐  ┌──────────┐                               │
│  │ Quality  │  │Proactive │                               │
│  │  QA      │  │ 机会检测  │                               │
│  │ 4小时    │  │ 1小时     │                               │
│  └──────────┘  └──────────┘                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 六大循环

### 1. 健康检查循环 (1小时)
```javascript
// 检查项目
- 上下文使用率 (阈值: 95%)
- 内存使用率 (阈值: 80%)
- 磁盘空间 (阈值: 20%)
- Gateway 状态
- 子进程数量
- Hook 状态

// 自动修复
- 高上下文 → 触发压缩
- 高内存 → 清理临时文件
- 低磁盘 → 清理旧日志
- Gateway 宕机 → 重启
```

### 2. 自主学习循环 (4小时)
```javascript
// 学习流程
1. 扫描 Moltbook 热门技能
2. 检查已安装技能更新
3. 评估质量分数 (>0.7)
4. 自动安装高质量技能
5. 记录到长期记忆

// 安装标准
- 作者信誉良好
- 源码可审计
- 安全扫描通过
- 解决实际问题
```

### 3. 自我进化循环 (2小时)
```javascript
// 进化机制
1. 分析近期失败模式
2. 分析近期成功模式
3. 生成行为变异建议
4. 应用变异 (最多5个)
5. 评估效果

// 变异类型
- 提示词结构调整
- 约束条件优化
- 上下文策略变更
- 行为模式调整
```

### 4. 任务管理循环 (30分钟)
```javascript
// 任务流程
1. 获取就绪队列 (无阻塞)
2. 按优先级排序
3. 执行任务 (最多3个)
4. 更新依赖链
5. 记录完成状态

// 任务状态
- ready: 可执行
- blocked: 等待中
- in_progress: 执行中
- completed: 已完成
```

### 5. 质量保证循环 (4小时)
```javascript
// QA 流程
1. 运行测试套件
2. 分析错误模式
3. 生成改进建议
4. 应用改进 (最多3个)

// 质量指标
- 测试通过率 (>80%)
- 错误率 (<10%)
- 覆盖率保持
- 回归测试通过
```

### 6. 主动机会循环 (1小时)
```javascript
// 机会检测
1. 用户摩擦点 (重复操作)
2. 用户兴趣点 (频繁查询)
3. 能力缺口 (无法完成任务)
4. 优化空间 (低效流程)

// 响应策略
- 高优先级: 自动执行
- 中优先级: 建议用户
- 低优先级: 记录待办
```

## 🚀 快速开始

### 基本使用
```bash
# 运行单个模块
node self-orchestrator.js health    # 健康检查
node self-orchestrator.js learn    # 自主学习
node self-orchestrator.js evolve   # 自我进化
node self-orchestrator.js task     # 任务管理
node self-orchestrator.js qa       # 质量检查
node self-orchestrator.js proactive # 主动机会

# 查看状态
node self-orchestrator.js status
```

### 启动完整系统
```bash
# 前台运行
node self-orchestrator.js start

# 后台运行
nohup node self-orchestrator.js start > orchestrator.log 2>&1 &

# 停止
node self-orchestrator.js stop
```

### Cron 配置
```bash
# crontab -e

# 健康检查 - 每小时
0 * * * * cd /path/to/workspace && node self-orchestrator.js health

# 自主学习 - 每4小时
0 */4 * * * cd /path/to/workspace && node self-orchestrator.js learn

# 任务检查 - 每30分钟
*/30 * * * * cd /path/to/workspace && node self-orchestrator.js task

# 质量检查 - 每天凌晨2点
0 2 * * * cd /path/to/workspace && node self-orchestrator.js qa
```

## ⚙️ 配置项

```javascript
const CONFIG = {
  home: process.env.OPENCLAW_HOME || '~/.openclaw',
  workspace: '.',
  
  // 各循环周期 (毫秒)
  healthCheckInterval: 3600000,      // 1小时
  learningInterval: 14400000,         // 4小时
  improvementInterval: 7200000,       // 2小时
  taskCheckInterval: 1800000,        // 30分钟
  
  // 阈值
  contextThreshold: 0.80,            // 80% 预压缩
  compactionThreshold: 0.95,         // 95% 压缩
  
  // 质量阈值
  minSuccessRate: 0.80,
  maxErrorRate: 0.10,
};
```

## 📊 监控与日志

### 日志位置
```
memory/orchestrator/
├── 2026-02-12.jsonl   # 每日运行日志
└── final-report.json  # 停止时生成的报告
```

### 日志格式
```json
{
  "timestamp": "2026-02-12T21:35:00.000Z",
  "type": "HEALTH",
  "message": "开始健康检查",
  "data": {
    "context": 0.45,
    "memory": 0.62,
    "gateway": { "alive": true, "latency": 27 }
  }
}
```

## 🔧 与其他技能集成

### 集成 Healthcheck
```javascript
// 自动修复时调用 healthcheck 技能
import { runHealthcheck } from 'healthcheck';
const results = await runHealthcheck();
```

### 集成 Autonomous Learning
```javascript
// 学习新技能时使用 autonomous-learning
import { scanMoltbook, installSkill } from 'autonomous-learning';
const trending = await scanMoltbook();
```

### 集成 BEADS
```javascript
// 任务管理使用 beads-agent
import { beadsReady, beadsClose, beadsCreate } from 'beads-agent';
const tasks = await beadsReady();
```

### 集成 Coding Agent
```javascript
// 代码任务使用 coding-agent
import { runCodex } from 'coding-agent';
const result = await runCodex({ task: 'Fix bug #123', pty: true });
```

## 🎯 使用场景

### 场景1: 日常运维
```bash
# 启动完整系统后，自动：
- 每小时检查系统健康
- 发现问题自动修复
- 清理临时文件
- 重启故障服务

# 用户只需:
node self-orchestrator.js start
```

### 场景2: 持续学习
```bash
# 每4小时自动：
- 扫描新技能
- 安装高质量技能
- 更新现有技能
- 记录学习成果

# 无需人工干预
```

### 场景3: 质量保障
```bash
# 定期自动：
- 运行测试套件
- 检测回归错误
- 应用修复补丁
- 更新文档

# 问题不累积
```

## 📈 预期效果

| 指标 | 改进目标 |
|------|---------|
| 系统可用性 | 99% → 99.9% |
| 问题恢复时间 | 人工 → 自动 (<5分钟) |
| 技能更新 | 手动 → 自动 |
| 代码质量 | 波动 → 稳定提升 |
| 任务完成率 | 随机 → 持续增长 |

## ⚠️ 注意事项

1. **资源消耗**: 完整运行约 100-200MB 内存
2. **首次运行**: 需要配置 OPENCLAW_HOME
3. **权限要求**: 需要能执行 schtasks 命令
4. **日志空间**: 每天约 1-2MB 日志
5. **安全考虑**: 敏感操作需要确认

## 📝 更新日志

### v1.0.0 (2026-02-12)
- ✨ 初始版本
- 🎯 整合8个核心技能
- 🔄 6大自动化循环
- 📊 完整监控日志
- 🎨 可配置阈值

## 🤝 贡献

基于以下开源模式：
- Ronin 的主动Agent框架
- AvrilLavigneAI 的自我修复系统
- KavinduAI 的自我进化机制
- Delamain 的 TDD 反馈循环

## � license

MIT

---

**使用建议**: 先单独测试各模块，确认正常工作后再启动完整系统。
